## 环境

请使用工作区内由 uv 管理的 Python 虚拟环境。
安装依赖统一使用 `uv pip install ...`。
执行脚本统一使用 `source .venv/bin/activate && python ...`。

## 双语文档规范

所有文档都必须有中英文两个版本且语义保持一致。
根目录下的项目文档（如 `README` 和 `AGENTS`）使用成对文件：
- 英文写在 `*.md`
- 中文写在 `*.zh.md`

编号研究图谱文档使用按语言区分的目录树：
- 英文写在 `docs/en/<chapter>/<topic>/`
- 中文写在 `docs/zh/<chapter>/<topic>/`

如果某个主题被拆成多个小节文件，主题概览统一使用 `README.md`。小节文件使用第三级序号和 slug，例如 `03-bench.md`、`04-model.md`、`05-agent-harness.md`。
引言章是例外：它直接使用 `docs/<lang>/00-introduction/` 下的二级 Markdown 文件，例如 `01-landscape-structure.md`。

中英文编号文档必须使用相同的基础文件名。
`docs/zh/` 下的中文文件不要再添加 `.zh` 后缀。

任意一侧改动，都要在同一次编辑中同步另一侧。
修改文档时，必须先更新英文稿，再更新中文稿。
文档表达应符合各自语言习惯，避免生硬直译。
修改 AGENTS 时应保持约束与语义一致，但不要求逐字复写用户原话，可以用更自然流畅的表述。

## 调研与写作风格

调研优先使用英文资料和专业学术引用，尤其是会议论文。
重点关注近一年左右进展，同时覆盖综述与里程碑工作。
检索时使用中英文关键词，覆盖 PDF、PPT、网页等来源，但证据基础以英文资料为主。
执行和思考阶段用英文，最终产出用自然、地道的中文。

如果需要查看远程仓库，先按约定的 `/tmp` 规范路径检查本地副本；若已存在则在原目录执行 `git pull`，否则再克隆到该目录。
为了让不同 agent 在不同轮次里都能稳定复用和定位材料，`/tmp` 下统一使用以下目录格式：
- GitHub 仓库：`/tmp/github-repos/<owner>/<repo>`
- arXiv TeX 源：`/tmp/arxiv/<arxiv_id>`（例如 `/tmp/arxiv/1706.03762`）
如果需要项目内的临时目录来放草稿、中间文件或页面检查结果，统一使用工作区根目录下的 `.tmp/`，不要再另外创建随意命名的临时文件夹。
GitHub 路径需要去掉结尾的 `.git` 后缀，并复用规范化后的 owner/repo 目录。
arXiv 路径统一使用规范论文 ID 作为目录名；只有任务明确依赖某个版本时，才在目录名中保留版本后缀。
如果涉及 arXiv 论文，请先把 TeX 源码下载并解压到对应的规范 `/tmp` 目录，再深入阅读。
如需 Hugging Face 数据集，可直接使用 `datasets.load_dataset(...)` 拉取。
本工作区允许使用本地 `.cache`（例如 `~/.cache`）缓存。

## 论文引用写作规范

本项目在相关工作写作中统一遵循：
- 表述尽量精炼，避免冗长和重复。
- 优先在分句末尾就近给出引用。
- 避免把大量引用集中堆在整句句末。
- 若论文已有简短且公认的简称（如 SkillRL、SkillsBench），优先直接使用该简称；若标题过长或不适合行文，不要硬写全名，改用“有研究/有工作”等中性表达。
- 论文正文必须符合正式科学论文写作规范：在叙述性句子中避免使用 `/`、`+` 等符号式缩写，改用明确的连接词和并列表达。

## Skill 检索流程

当任务需要检索 skill 时，必须遵循以下流程：
- 同时使用 `npx skills find` 和 `npx clawhub search`，检索能够完成 **【目标功能/任务】** 的 skill。
- 还必须调用 SkillNet API，例如 `curl -s 'http://api-skillnet.openkg.cn/v1/search?q=paper%20rebuttal'`，并将其结果作为检索 **【目标功能/任务】** 的额外一等来源。
- 除技能市场外，还必须直接在 GitHub 上检索与 **【目标功能/任务】** 相关的仓库或 skill 目录；同样要尝试多组关键词，并将 GitHub 命中的结果视为后续实现核验的一等候选。
- `npx clawhub` 的搜索限流较严格，查询时注意降频，优先串行执行，避免短时间内并发发起多次搜索。
- SkillNet 接口同样是关键词检索，不是语义检索；必须尝试多组关键词，并把返回的 GitHub/blob/tree 链接规整到可验证、尽量稳定的公开入口地址。
- 先阅读 **【目标论文】**，再按该论文定义的任务边界来制定筛选标准。
- 这些检索接口都只是关键词检索，不是语义检索；必须使用多组关键词反复搜索。
- 如果返回多个候选 skill，必须启动并行子代理，对每个候选独立核验。
- 将候选仓库放到规范路径 `/tmp/github-repos/<owner>/<repo>` 下处理，但先检查每个候选是否已在本地 clone。若已存在则在原目录执行 `git pull` 后继续；不存在再 clone 到该目录。然后检查真实实现内容（如 `SKILL.md`、脚本、模板、辅助代码），再判断其是否真正符合 **【目标功能/任务】**。
- 确认最终匹配集合后，需要对每个 skill 给出具体设计总结。

## Agent 检索流程

当任务需要检索 agent 时，必须遵循以下流程：
- 先阅读目标 benchmark 或目标论文，再据此明确任务边界、输入输出形式和评测口径。
- 检索过程中可以进入每篇候选论文查看 baseline，对比其与已有方法的关系、提升来源和评测设置。
- 可以继续查找后续引用这些论文的工作，用来识别后续高影响力方法、变体和延伸方向。
- 筛选时优先保留 rich prompt、tools、workflow 编排、记忆机制或多 Agent 协作驱动的方法。
- 若一篇工作的主要贡献在训练算法、数据规模扩展、预训练或后训练配方，而 agent 设计本身较弱，则不要把它作为本任务中的核心 agent 候选。
- 若返回多个候选 agent，需要结合论文、项目主页和真实仓库实现交叉核验，而不是只看榜单分数或摘要表述。

## `sota-agents-and-skills` 撰写规范

更新该文档时，必须遵循以下规则：
- 对所有 Agent：在名称上放论文超链接，然后在括注中给出开源代码 URL（如“开源代码：URL”）。
- 只要存在论文，必须主动检索并使用对应的 arXiv 链接。
- 如果明确没有论文，则 Agent 名称不保留超链接。
- 对所有 Skill：在名称上给出超链接（`skills.sh`、`clawhub` 或其他官方来源）。
- 必须检查该文件中本次新增 URL 是否可访问；如果新增链接失效，提交前完成修复或移除。
